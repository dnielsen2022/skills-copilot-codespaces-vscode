// Create web server
// 1. Load express module
const express = require("express");
// 2. Create an express app
const app = express();
// 3. Load the comments.json file
const comments = require("./comments.json");
// 4. Create a route handler for GET /comments
app.get("/comments", function (req, res) {
  res.send(comments);
});
// 5. Start the server
app.listen(3000, () => console.log("Server is listening on port 3000..."));

// Path: comments.js
// Create web server
// 1. Load express module
const express = require("express");
// 2. Create an express app
const app = express();
// 3. Load the comments.json file
const comments = require("./comments.json");
// 4. Create a route handler for GET /comments
app.get("/comments", function (req, res) {
  res.send(comments);
});
// 5. Start the server
app.listen(3000, () => console.log("Server is listening on port 3000..."));

// Path: comments.js
// Create web server
// 1. Load express module
const express = require("express");
// 2. Create an express app
const app = express();
// 3. Load the comments.json file
const comments = require("./comments.json");
// 4. Create a route handler for GET /comments
app.get("/comments", function (req, res) {
  res.send(comments);
});
// 5. Start the server
app.listen(3000, () => console.log("Server is listening on port 3000..."));

// Path: comments.js
// Create web server
// 1. Load express module
const express = require("express");
// 2. Create an express app
const app = express();
// 3. Load the comments.json file
const comments = require("./comments.json");
// 4. Create a route handler for GET /comments
app.get("/comments", function (req, res) {
  res.send(comments);
});
// 5. Start the server
app.listen(3000, () => console.log("Server is listening on port 3000..."));

// Path: comments.js
// Create web server
// 1. Load express module
const express = require("express");
// 2. Create an express app